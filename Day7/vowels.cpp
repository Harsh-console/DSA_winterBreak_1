#include<bits/stdc++.h>
using namespace std;

int xor_square(vector<int> &word){
    int n = word.size();
    for(int mask = 0; mask < (1<<n); mask++){
        
    }
}

int main(){
    int n;
    cin>>n;
    vector<int> word(n);
    for(int x : word) cin>>x;

}
